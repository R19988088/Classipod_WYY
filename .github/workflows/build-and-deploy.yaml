name: Android Release APK

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: android-release
  cancel-in-progress: true

jobs:
  android:
    name: Signed arm64 APK
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: subosito/flutter-action@v2
        with:
          flutter-version-file: pubspec.yaml
          channel: stable
          cache: true

      - name: Install dependencies and verify
        run: |
          flutter pub get --suppress-analytics
          flutter gen-l10n
          dart run build_runner build --delete-conflicting-outputs
          flutter analyze --suppress-analytics
          flutter test --no-pub --suppress-analytics

      - name: Restore fixed signing identity
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          STORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          test -n "$KEYSTORE_BASE64"
          printf '%s' "$KEYSTORE_BASE64" | base64 --decode > android/key.jks
          cat > android/key.properties <<EOF
          storePassword=$STORE_PASSWORD
          keyPassword=$KEY_PASSWORD
          keyAlias=$KEY_ALIAS
          storeFile=../key.jks
          EOF

      - name: Build minimal release APK
        run: |
          flutter build apk \
            --release \
            --flavor production \
            --target-platform android-arm64 \
            --split-per-abi \
            --obfuscate \
            --split-debug-info=/tmp/classipod-symbols \
            --tree-shake-icons \
            --no-pub \
            --suppress-analytics
          mv build/app/outputs/flutter-apk/app-arm64-v8a-production-release.apk Classipod-WYY.apk

      - name: Verify package and fixed certificate
        run: |
          test -s Classipod-WYY.apk
          BUILD_TOOLS=$(find "$ANDROID_SDK_ROOT/build-tools" -mindepth 1 -maxdepth 1 -type d | sort -V | tail -1)
          "$BUILD_TOOLS/aapt2" dump badging Classipod-WYY.apk | grep "package: name='com.adeeteya.classipod'"
          "$BUILD_TOOLS/apksigner" verify --verbose --print-certs Classipod-WYY.apk | tee /tmp/apksigner.txt
          CERT=$(sed -n 's/.*certificate SHA-256 digest: //p' /tmp/apksigner.txt | head -1)
          test "$CERT" = "0aafc0ba0891648561efcc32ea2215b84d144594f64f35b2d8e31b7eb803c0b1"
          test "$(unzip -Z1 Classipod-WYY.apk | awk -F/ '/^lib\// {print $2}' | sort -u)" = "arm64-v8a"
          ! unzip -Z1 Classipod-WYY.apk | grep -E 'DebugProbes|main_development|device_preview|symbols'
          sha256sum Classipod-WYY.apk

      - name: Publish direct APK
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view continuous >/dev/null 2>&1; then
            gh release edit continuous --title "Classipod WYY Continuous" --prerelease
          else
            gh release create continuous --title "Classipod WYY Continuous" --prerelease
          fi
          gh release upload continuous Classipod-WYY.apk --clobber
